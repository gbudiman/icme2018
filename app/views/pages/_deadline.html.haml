:ruby
  if !target.is_a?(Array)
    target = [target]
  end

%h3 Important Dates
%ul
  - target.each do |t|
    - data = DateHelper::get spec: t
    %li #{data[:title]} proposal submission deadline: #{data[:proposal]}
    - if data[:notification]
      %li #{data[:title]} proposal acceptance notification: #{data[:notification]}